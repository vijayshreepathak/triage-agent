# --- Runtime ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4
python-dotenv>=1.0

# --- Orchestration ---
langgraph>=0.2.60
langchain-core>=0.3.30

# --- LLM providers (Factory pattern selects one at runtime) ---
langchain-anthropic>=0.3.0     # Claude (default provider)
langchain-groq>=0.2.0
langchain-openai>=0.2.0        # used for OpenRouter (OpenAI-compatible API)

# --- MCP (SEARCH_PROVIDER=mcp: search via any MCP server) ---
mcp>=1.0.0

# --- Search providers (Strategy pattern behind SearchTool interface) ---
tavily-python>=0.5.0
ddgs>=9.0.0                    # DuckDuckGo, no API key required

# --- HTTP ---
httpx>=0.27

# --- Persistence (SQLite by default; Postgres via DATABASE_URL) ---
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20
asyncpg>=0.30

# --- Auth (Clerk JWT verification; PyJWT+cryptography also come with mcp) ---
pyjwt[crypto]>=2.9

# --- Dev / test ---
pytest>=8.2
pytest-asyncio>=0.23
pytest-cov>=5.0
respx>=0.21
black>=24.4
ruff>=0.5
